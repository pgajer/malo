# malo

`malo` is a transitional package for legacy 1D non-linear regression APIs that
are being extracted from `gflow`.

In the initial extraction phase, `malo` preserves familiar function names and
forwards calls to the corresponding `gflow` implementations.
