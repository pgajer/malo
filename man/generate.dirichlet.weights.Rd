% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magelo_external_bb.R
\name{generate.dirichlet.weights}
\alias{generate.dirichlet.weights}
\title{Generate Dirichlet(1,...,1) Weights for Bayesian Bootstrap}
\usage{
generate.dirichlet.weights(n, n.BB, seed = NULL)
}
\arguments{
\item{n}{Integer. Number of data points (length of weight vectors).}

\item{n.BB}{Integer. Number of BB iterations (columns).}

\item{seed}{Integer or NULL. Random seed for reproducibility.}
}
\value{
A numeric matrix of dimension n x n.BB, where each column contains
Dirichlet(1,...,1) weights scaled to sum to n.
}
\description{
Generates a matrix of Dirichlet weights that can be used for paired
Bayesian bootstrap comparisons. Each column contains weights that sum to n
(not normalized to 1, as BB convention uses n * w).
}
\details{
The Dirichlet(1,...,1) distribution is uniform on the (n-1)-dimensional
simplex. Weights are generated by:
\enumerate{
\item Draw n independent Exponential(1) random variables
\item Normalize to sum to n
}

The resulting weights have mean 1 for each element, which is the expected
value under uniform sampling.
}
\examples{
\dontrun{
# Generate 100 weight vectors for 50 data points
lambda <- generate.dirichlet.weights(50, 100, seed = 123)

# Verify columns sum to n
colSums(lambda)  # Should all be 50
}

}
