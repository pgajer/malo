% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_utils.R
\name{get.svm.MAB.xD}
\alias{get.svm.MAB.xD}
\title{Estimate Mean Absolute Bias (MAB) of SVM Model for Multi-dimensional Data}
\usage{
get.svm.MAB.xD(X, y, yt, folds, cost, gamma, y.binary = FALSE)
}
\arguments{
\item{X}{A matrix or data frame of predictor variables (dimension > 1).}

\item{y}{A numeric vector of response values.}

\item{yt}{A numeric vector of true response values for evaluation.}

\item{folds}{A list of indices for cross-validation folds.}

\item{cost}{A numeric vector of candidate cost values to evaluate.}

\item{gamma}{A numeric vector of candidate gamma values for the radial basis kernel.}

\item{y.binary}{Logical indicating if y is binary (default = FALSE).}
}
\value{
A list containing:
\describe{
\item{MAB}{Mean Absolute Bias calculated on the dataset}
\item{RMSE}{Root Mean Square Error}
\item{predictions}{Predictions made by the SVM model}
\item{residuals}{Absolute residuals}
\item{model}{The fitted SVM model object}
\item{parameters}{List containing optimal cost and gamma values}
}
}
\description{
Fits a Support Vector Machine (SVM) model to multi-dimensional data and evaluates
its performance using cross-validation. The optimal hyperparameters are selected
based on cross-validated MAE.
}
\details{
The function performs grid search over cost and gamma parameters using
cross-validation to find the optimal values. It then fits a final SVM model
with these parameters and evaluates on the training data (in-sample).
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
n <- 100
X <- matrix(rnorm(n * 3), ncol = 3)
y <- X[,1] + X[,2]^2 + rnorm(n, sd = 0.1)
yt <- X[,1] + X[,2]^2  # True values

# Create cross-validation folds
folds <- create.folds(y, k = 10, list = TRUE, returnTrain = TRUE)

# Define hyperparameter grid
cost <- 10^seq(-1, 1, length.out = 3)
gamma <- 10^seq(-1, 1, length.out = 3)

# Fit SVM model and compute MAB
result <- get.svm.MAB.xD(X, y, yt, folds, cost, gamma)
print(result$MAB)
}

}
