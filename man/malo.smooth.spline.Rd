% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline_replacements.R
\name{malo.smooth.spline}
\alias{malo.smooth.spline}
\title{Robust Smoothing Spline Wrapper}
\usage{
malo.smooth.spline(
  x,
  y,
  w = NULL,
  df = NULL,
  spar = NULL,
  use.gcv = TRUE,
  df.max = NULL,
  cv.folds = 5L,
  cv.repeats = 3L,
  spar.grid = NULL,
  cv.one.se = TRUE,
  cv.seed = NULL,
  reuse.spar = NULL
)
}
\arguments{
\item{x}{Numeric vector of predictor values.}

\item{y}{Numeric vector of response values.}

\item{w}{Optional non-negative numeric weights.}

\item{df}{Optional fixed degrees of freedom passed to
\code{\link[stats:smooth.spline]{stats::smooth.spline()}}.}

\item{spar}{Optional fixed smoothing parameter passed to
\code{\link[stats:smooth.spline]{stats::smooth.spline()}}.}

\item{use.gcv}{Logical; if \code{TRUE}, use \code{smooth.spline} automatic GCV when
neither \code{df} nor \code{spar} is fixed.}

\item{df.max}{Optional upper bound on effective degrees of freedom.}

\item{cv.folds}{Number of CV folds when \code{use.gcv = FALSE}.}

\item{cv.repeats}{Number of repeated CV splits when \code{use.gcv = FALSE}.}

\item{spar.grid}{Optional candidate \code{spar} grid for robust CV selection.}

\item{cv.one.se}{Logical; if \code{TRUE}, use a 1-SE rule after repeated CV.}

\item{cv.seed}{Optional seed for CV reproducibility.}

\item{reuse.spar}{Optional fixed \code{spar} value to reuse (e.g. across BB
replicates).}
}
\value{
A \code{smooth.spline} fit object or \code{NULL} if fitting is not possible.
Selection metadata is attached as \code{fit$gflow.selection}.
}
\description{
User-facing wrapper around \code{\link[stats:smooth.spline]{stats::smooth.spline()}} with optional robust
repeated K-fold cross-validation over a \code{spar} grid.
}
