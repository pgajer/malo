% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mabilo.R
\name{plot.mabilo}
\alias{plot.mabilo}
\title{Plot Method for Mabilo Objects}
\usage{
\method{plot}{mabilo}(
  x,
  type = "fit",
  title = "",
  xlab = "",
  ylab = "",
  with.y.true = TRUE,
  with.pts = FALSE,
  with.CrI = TRUE,
  true.col = "red",
  ma.col = "blue",
  pts.col = "gray60",
  with.predictions.pts = FALSE,
  predictions.pts.col = "blue",
  predictions.pts.pch = 20,
  CrI.as.polygon = TRUE,
  CrI.polygon.col = "gray85",
  CrI.line.col = "gray10",
  CrI.line.lty = 2,
  ylim = NULL,
  legend.cex = 0.8,
  k = NULL,
  with.legend = TRUE,
  legend.pos = "topright",
  ...
)
}
\arguments{
\item{x}{An object of class "mabilo"}

\item{type}{Character string specifying the type of plot to create (partial matching supported):
\itemize{
\item "fit": Plot fitted values against x values (e.g., "f" or "fi")
\item "diagnostic": Plot error diagnostics for different k values (e.g., "d" or "dia")
\item "residuals": Plot residuals against x values (e.g., "res")
\item "residuals.hist": Plot histogram of residuals (e.g., "rh" or "residuals.h")
}}

\item{title}{Plot title (default: "")}

\item{xlab}{Label for x-axis (default: "")}

\item{ylab}{Label for y-axis (default: "")}

\item{with.y.true}{Logical; if TRUE and true values are available, adds them to the plot
(default: TRUE)}

\item{with.pts}{Logical; if TRUE, adds original data points to the plot
(default: FALSE)}

\item{with.CrI}{Logical; if TRUE and bootstrap results available, adds credible intervals
(default: TRUE)}

\item{true.col}{Color for true values line (default: "red")}

\item{ma.col}{Color for predictions (default: "blue")}

\item{pts.col}{Color for data points (default: "gray60")}

\item{with.predictions.pts}{Logical; if TRUE, adds points at prediction values
(default: FALSE)}

\item{predictions.pts.col}{Color for prediction points (default: "blue")}

\item{predictions.pts.pch}{Point character for prediction points (default: 20)}

\item{CrI.as.polygon}{Logical; if TRUE, draws credible intervals as a polygon
(default: TRUE)}

\item{CrI.polygon.col}{Color for credible interval polygon (default: "gray85")}

\item{CrI.line.col}{Color for credible interval lines (default: "gray10")}

\item{CrI.line.lty}{Line type for credible interval lines (default: 2)}

\item{ylim}{Numeric vector of length 2 giving y-axis limits (default: NULL)}

\item{legend.cex}{Character expansion factor for legend (default: 0.8)}

\item{k}{Specific k value to plot predictions for (default: NULL, uses optimal k)}

\item{with.legend}{Logical; if TRUE, displays legend on the plot (default: TRUE)}

\item{legend.pos}{Character string specifying legend position (default: "topright")}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
Invisibly returns NULL. The function is called for its side effect of producing plots.
}
\description{
Generates diagnostic and visualization plots for mabilo objects. The function
supports different types of plots including fitted values with optional credible
intervals, error diagnostics, and residual analyses.
}
\details{
The function produces different types of plots:

For type = "fit":
\itemize{
\item Plots the fitted values against x values
\item Optionally includes true values and data points
\item Shows credible intervals if bootstrap results available
\item Supports customization of colors and styles
}

For type = "diagnostic":
\itemize{
\item Shows LOOCV error diagnostics for different k values
\item Indicates optimal k value with vertical line
\item Shows true errors if available
}

For type = "residuals" and "residuals.hist":
\itemize{
\item Visualizes model residuals in various ways
\item Includes reference lines and density curves
}
}
\examples{
# Generate example data
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)

# Fit mabilo model
fit <- mabilo(x, y, k.min = 3, k.max = 10, n.bb = 0)

# Basic fit plot with credible intervals
plot(fit)

# Diagnostic plot showing errors
plot(fit, type = "diagnostic")

# Residual plot
plot(fit, type = "residuals")

}
\seealso{
\code{\link{mabilo}} for fitting the mabilo model
}
