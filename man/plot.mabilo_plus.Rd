% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mabilo_plus.R
\name{plot.mabilo_plus}
\alias{plot.mabilo_plus}
\title{Plot Method for Mabilo Plus Objects}
\usage{
\method{plot}{mabilo_plus}(
  x,
  type = "fit",
  title = "",
  xlab = "",
  ylab = "",
  predictions.type = "both",
  diagnostic.type = "both",
  with.y.true = TRUE,
  with.pts = FALSE,
  true.lwd = 2,
  true.col = "red",
  sm.col = "#00FF00",
  ma.col = "blue",
  pts.col = "gray60",
  with.predictions.pts = FALSE,
  predictions.pts.col = "blue",
  predictions.pts.pch = 20,
  ylim = NULL,
  legend.cex = 0.8,
  k = NULL,
  with.legend = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class "mabilo_plus"}

\item{type}{Character string specifying the type of plot to create (partial matching supported):
\itemize{
\item "fit": Plot fitted values against x values (e.g., "f" or "fi")
\item "diagnostic": Plot error diagnostics for different k values (e.g., "d" or "dia")
\item "residuals": Plot residuals against x values (e.g., "res")
\item "residuals.hist": Plot histogram of residuals (e.g., "rh" or "residuals.h")
}}

\item{title}{Plot title (default: "")}

\item{xlab}{Label for x-axis (default: "")}

\item{ylab}{Label for y-axis (default: "")}

\item{predictions.type}{Type of predictions to plot:
\itemize{
\item "both": plot both sm_predictions and ma_predictions
\item "sm": sm_predictions only
\item "ma": ma_predictions only
}}

\item{diagnostic.type}{Type of errors to show in diagnostic plot:
\itemize{
\item "both": show both sm and ma errors
\item "sm": sm errors only
\item "ma": ma errors only
}}

\item{with.y.true}{Logical; if TRUE and true values are available, adds them to the plot
(default: TRUE)}

\item{with.pts}{Logical; if TRUE, adds original data points to the plot
(default: FALSE)}

\item{true.lwd}{Line width for true values (default: 2)}

\item{true.col}{Color for true values line (default: "red")}

\item{sm.col}{Color for sm prediction and error lines (default: "#00FF00")}

\item{ma.col}{Color for ma prediction and error lines (default: "blue")}

\item{pts.col}{Color for points (default: "gray60")}

\item{with.predictions.pts}{Logical; if TRUE, adds points at prediction values
(default: FALSE)}

\item{predictions.pts.col}{Color for prediction points (default: "blue")}

\item{predictions.pts.pch}{Point character for prediction points (default: 20)}

\item{ylim}{Numeric vector of length 2 giving y-axis limits; if NULL, computed from data
(default: NULL)}

\item{legend.cex}{Character expansion factor for legend (default: 0.8)}

\item{k}{Specific k value to plot predictions for (default: NULL, uses optimal k)}

\item{with.legend}{Logical; if TRUE, displays legend on the plot (default: TRUE)}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
Invisibly returns NULL. The function is called for its side effect of producing plots.
}
\description{
Generates various diagnostic and visualization plots for mabilo.plus objects.
The function supports different types of plots including fitted values,
diagnostic plots, and residual analyses. For each plot type, various
customization options are available through the function parameters.
}
\examples{
\dontrun{
# Generate example data
x <- seq(0, 10, length.out = 100)
y <- sin(x) + rnorm(100, 0, 0.1)

# Fit mabilo.plus model
fit <- mabilo.plus(x, y)

# Basic fit plot
plot(fit)

# Plot both SM and MA predictions
plot(fit, type = "fit", predictions.type = "both")

# Diagnostic plot
plot(fit, type = "diagnostic", diagnostic.type = "both")

# Residual plots
plot(fit, type = "residuals", predictions.type = "ma")
plot(fit, type = "residuals.hist", predictions.type = "both")
}

}
